/*
  initially stored location and values of number line points in a 2D array. Works, but complex and error-prone
  
  will try (dimanesion updated every draw cycle to account for screen size changes:
   - for vertical locations; store mininmum, step size (in pixels) and starting height (as screen proportion).
   - for horizontal locations; store number line location (as proportion of screen width) and step size (in pixels)
*/
